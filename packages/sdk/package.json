{
  "name": "@settle/sdk",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "codegen": "tsx codama.config.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured yet'",
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist .turbo src/generated"
  },
  "dependencies": {
    "@settle/types": "workspace:*",
    "@solana/kit": "^3.0.0",
    "@noble/hashes": "^1.5.0",
    "@noble/curves": "^1.6.0",
    "@noble/ciphers": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
